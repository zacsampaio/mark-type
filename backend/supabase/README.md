# Supabase (SQL legado)

As migrations oficiais para **CLI + Docker local** estão na raiz do monorepo:

`supabase/migrations/`

Use `supabase start` a partir da pasta raiz `doccraft/` (ver README principal, secção Supabase local).
